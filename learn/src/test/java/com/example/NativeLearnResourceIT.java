package com.example;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeLearnResourceIT extends LearnResourceTest {

    // Execute the same tests but in native mode.
}
